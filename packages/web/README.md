# persona-web

Next.js web application for Persona.

## Setup

```bash
cd packages/web
npm install
npm run dev
```

## Notes

The Next.js scaffold will be created during Spec 09 (month 3).
This directory is a placeholder until then.
